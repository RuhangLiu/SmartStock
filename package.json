{
  "name": "smartstock-azure-app",
  "version": "1.0.0",
  "private": true,
  "description": "Azure App Service entry point for the SmartStock React and Express application",
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "scripts": {
    "build": "npm ci --include=dev --prefix client && npm run build --prefix client",
    "start": "node server/index.js",
    "test": "npm test --prefix server"
  }
}
