{
  "name": "smartstock-azure-app",
  "version": "1.0.0",
  "private": true,
  "description": "Azure App Service entry point for the SmartStock React and Express application",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm ci --include=dev --prefix client && npm run build --prefix client && npm ci --omit=dev --prefix server",
    "start": "node server/index.js",
    "test": "npm test --prefix server"
  }
}
